// How this mod presents itself in the launcher and the in-game mod list.
// Without it DayZ shows the folder name and nothing else. The picture and
// logo paths are left out on purpose: DayZ draws a blank for a missing
// texture and says nothing, so a wrong path is worse than none.

name        = "OpenZone Core VPP";
author      = "Zone Protocol";
authorID    = "";
version     = "0.1";
description = "Companion mod. The OpenZone tab inside VPP Admin Tools. REQUIRES OpenZone Core and VPP Admin Tools -- without both the game refuses to load. github.com/covalschi/openzone-core";
tooltip     = "OpenZone Core VPP";
overview    = "The OpenZone admin tab for VPP Admin Tools. Install only alongside OpenZone Core and VPP Admin Tools.";
