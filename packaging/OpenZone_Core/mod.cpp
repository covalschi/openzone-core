// How this mod presents itself in the launcher and the in-game mod list.
// Without it DayZ shows the folder name and nothing else. The picture and
// logo paths are left out on purpose: DayZ draws a blank for a missing
// texture and says nothing, so a wrong path is worse than none.

name        = "OpenZone Core";
author      = "Zone Protocol";
authorID    = "";
version     = "0.1";
description = "The framework of the OpenZone series: services, the bridge client, the admin window and the design tokens. Every OpenZone mod runs on top of it. github.com/covalschi/openzone-core";
tooltip     = "OpenZone Core";
overview    = "The core of the OpenZone series. Needs Community Framework. Every other OpenZone mod requires it.";
