// GENERATED by dayz-mcp layout_build from ui/OpenZone_VPP/oz_vpp_mirror_row.json -- edit the source, not this file.
// One Discord mirror toggle of the RAW JSON pane, instantiated once per mirrorable kind into MirrorStrip. THE KIND NAMES ARE NOT HERE and must never be: they come from the mods that registered the kinds (OZ_MirrorOps.List), so the core neither names another mod's kind nor caps how many there may be. The script writes the caption -- '<KIND>: ON', '<KIND>: OFF', 'PRESS AGAIN' -- into MirrorRowBtnText, lays the rows along the strip and shrinks them when they no longer fit; the name MirrorRowBtnText is a contract with OZ_VppMenu.c. A button ROOT holds page content instead of the Edge/Bg/Text idiom a nested button gets, so the three children are written out here -- with the names that idiom would have given them, which is what ShrinkTab walks.
ButtonWidgetClass MirrorRowBtn {
 visible 1
 position 0 0
 size 180 30
 hexactpos 1
 vexactpos 1
 hexactsize 1
 vexactsize 1
 text ""
 {
  // One unit outside the button on every side: 180x30 plus two.
  PanelWidgetClass MirrorRowBtnEdge {
   visible 1
   ignorepointer 1
   position -1 -1
   size 182 32
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.22 0.3 0.38 1
   priority 0
   style rover_sim_colorable
  }
  PanelWidgetClass MirrorRowBtnBg {
   visible 1
   ignorepointer 1
   position 0 0
   size 180 30
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.135 0.18 0.225 1
   priority 1
   style rover_sim_colorable
  }
  // Empty: the caption is the kind and its state, written by PaintMirror.
  TextWidgetClass MirrorRowBtnText {
   visible 1
   ignorepointer 1
   position 0 0
   size 180 30
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.31 0.71 0.91 1
   priority 2
   text ""
   font "gui/fonts/sdf_MetronBook24"
   "exact text" 1
   "exact text size" 13
   "text halign" center
   "text valign" center
  }
 }
}
