// GENERATED by dayz-mcp layout_build from ui/OpenZone_VPP/oz_vpp_tab.json -- edit the source, not this file.
// One tab button of the admin window, instantiated once per registered pane into TabStrip. The script renames the root to 'tab:<id>' and writes the caption into OZ_VppTabText, so both names are a contract with OZ_VppMenu.c. A button ROOT holds page content instead of the Edge/Bg/Text idiom a nested button gets, so the three children are written out here -- with the names that idiom would have given them.
ButtonWidgetClass OZ_VppTab {
 visible 1
 position 0 0
 size 150 28
 hexactpos 1
 vexactpos 1
 hexactsize 1
 vexactsize 1
 text ""
 {
  // One unit outside the button on every side: vpp.tab (150x28) plus two.
  PanelWidgetClass OZ_VppTabEdge {
   visible 1
   ignorepointer 1
   position -1 -1
   size 152 30
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.22 0.3 0.38 1
   priority 0
   style rover_sim_colorable
  }
  PanelWidgetClass OZ_VppTabBg {
   visible 1
   ignorepointer 1
   position 0 0
   size 150 28
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.135 0.18 0.225 1
   priority 1
   style rover_sim_colorable
  }
  // Empty: the caption is the pane's label, set by RegisterPane; ShowPane recolours it accent for the open tab and muted for the rest.
  TextWidgetClass OZ_VppTabText {
   visible 1
   ignorepointer 1
   position 0 0
   size 150 28
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.31 0.71 0.91 1
   priority 2
   text ""
   font "gui/fonts/sdf_MetronBook24"
   "exact text" 1
   "exact text size" 13
   "text halign" center
   "text valign" center
  }
 }
}
