// GENERATED by dayz-mcp layout_build from ui/OpenZone_VPP/oz_vpp_admin.json -- edit the source, not this file.
// The OpenZone admin window, hosted by VPP Admin Tools. The root places itself at a fraction of the host (at_frac) at an exact size, because VPP hands it a workspace, not a page. Only the window chrome lives here: the background, the title, the close button and TabStrip -- the empty strip the script fills with one tab template per registered pane. THE PANES ARE NOT HERE: every pane, the core's three and the glue mods' own, is its own layout the script creates under this root at [0, vpp.paneY], so a mod that adds a pane needs nothing from this file. Names OZ_VppRoot, BtnVppClose and TabStrip are read by OZ_VppMenu.c.
// Hidden: VPP shows the window itself (ShowSubMenu right after OnCreate). Priority 10 above VPP's own frames, as before.
FrameWidgetClass OZ_VppRoot {
 visible 0
 position 0.24 0.14
 size 1000 620
 hexactpos 0
 vexactpos 0
 hexactsize 1
 vexactsize 1
 priority 10
 {
  PanelWidgetClass VppBgEdge {
   visible 1
   ignorepointer 1
   position -1 -1
   size 1002 622
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.22 0.3 0.38 1
   priority 1
   style rover_sim_colorable
  }
  // The window's ground and its one-unit border, the same pair the PDA shell paints; priority 1 leaves 2 for the panes the script creates on top of it.
  PanelWidgetClass VppBg {
   visible 1
   ignorepointer 1
   position 0 0
   size 1000 620
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   color 0.055 0.075 0.095 1
   priority 1
   style rover_sim_colorable
  }
  FrameWidgetClass VppHeader {
   visible 1
   position 0 0
   size 1000 96
   hexactpos 1
   vexactpos 1
   hexactsize 1
   vexactsize 1
   priority 3
   {
    TextWidgetClass VppTitle {
     visible 1
     ignorepointer 1
     position 20 14
     size 500 26
     hexactpos 1
     vexactpos 1
     hexactsize 1
     vexactsize 1
     color 0.31 0.71 0.91 1
     priority 0
     text "OPENZONE ADMIN"
     font "gui/fonts/sdf_MetronBook24"
     "exact text" 1
     "exact text size" 18
     "text halign" left
     "text valign" center
    }
    ButtonWidgetClass BtnVppClose {
     visible 1
     halign right_ref
     position 14 12
     size 34 34
     hexactpos 1
     vexactpos 1
     hexactsize 1
     vexactsize 1
     text ""
     priority 1
     {
      PanelWidgetClass BtnVppCloseEdge {
       visible 1
       ignorepointer 1
       position -1 -1
       size 36 36
       hexactpos 1
       vexactpos 1
       hexactsize 1
       vexactsize 1
       color 0.22 0.3 0.38 1
       priority 0
       style rover_sim_colorable
      }
      PanelWidgetClass BtnVppCloseBg {
       visible 1
       ignorepointer 1
       position 0 0
       size 34 34
       hexactpos 1
       vexactpos 1
       hexactsize 1
       vexactsize 1
       color 0.135 0.18 0.225 1
       priority 1
       style rover_sim_colorable
      }
      TextWidgetClass BtnVppCloseText {
       visible 1
       ignorepointer 1
       position 0 0
       size 34 34
       hexactpos 1
       vexactpos 1
       hexactsize 1
       vexactsize 1
       color 0.31 0.71 0.91 1
       priority 2
       text "X"
       font "gui/fonts/sdf_MetronBook24"
       "exact text" 1
       "exact text size" 16
       "text halign" center
       "text valign" center
      }
     }
    }
    // Empty on purpose: LayoutTabs puts one oz_vpp_tab.layout in it per registered pane, so the strip is empty in the gallery. 28 is the height of vpp.tab and 52 puts its bottom at 80, inside the vpp.header band (96) and clear of the panes at vpp.paneY -- written out because the generator does no arithmetic on tokens.
    FrameWidgetClass TabStrip {
     visible 1
     position 0 52
     size 1000 28
     hexactpos 1
     vexactpos 1
     hexactsize 1
     vexactsize 1
     priority 2
    }
   }
  }
 }
}
