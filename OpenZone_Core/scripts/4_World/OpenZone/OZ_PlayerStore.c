// Дані гравця: один файл на Steam64 у $profile:OpenZone\players\.
//
// Чому НЕ CF_ModStorage: він прив'язаний до сутності й помирає разом із
// персонажем. Друзі, мітки й прив'язка до Discord помирати не повинні --
// це власність акаунта, а не тіла.
//
// Запис відкладений: пачкою й на дисконекті, а не на кожну зміну. Гравців
// може бути вісімдесят, а змін -- десятки на хвилину в кожного.

// Одна вхідна пропозиція обміну контактами разом зі своїм строком.
//
// Окремий тип, а не рядок із суфіксом: строк -- це друге поле, і склеїти
// його з ключем означало б, що кожне порівняння ключів мусить пам'ятати про
// суфікс. Перше ж місце, яке про нього забуде, перестане знаходити друга.
class OZ_FriendReq
{
    // Ключ ПЕРСОНАЖА того, хто попросив: "<uid>#<покоління>".
    string Key = "";

    // Коли пропозиція протухне -- календарем UTC, у форматі OZ_Time.
    // Календарем, а не часом рушія: рушійний лічильник починається з нуля
    // після кожного рестарту, і саме тому строк узагалі не переживав його.
    string Until = "";

    // Копія у створений СКРИПТОМ об'єкт -- шапка OZ_ConfigBase. Запис
    // гравця, який зараз онлайн, лежить у кеші до дисконекту, тобто його
    // пропозиції читають годинами після розбору файла.
    OZ_FriendReq Copy()
    {
        OZ_FriendReq c = new OZ_FriendReq();
        c.Key   = Key;
        c.Until = Until;
        return c;
    }
}

class OZ_PlayerData : OZ_ConfigBase
{
    string SteamId   = "";
    string DiscordId = "";
    string FirstSeen = "";
    string LastSeen  = "";

    // Ім'я з останнього входу. Кеш, а не джерело правди: воно потрібне, щоб
    // показати запит у друзі від того, кого зараз немає на сервері. Без
    // кешу довелось би або показувати Steam64 замість імені, або мовчати про
    // офлайнових зовсім.
    string Name = "";

    // --- останнє, що ми про нього знали ---
    //
    // Знімок ролей із останнього разу, коли гравець був у мережі. Це кеш
    // ПОКАЗУ, того ж роду, що й Name вище: щоб офлайновий контакт у списку
    // не перетворювався на голе ім'я без фракції й звання.
    //
    // Окремі поля, а НЕ BaseFaction/OrgFaction нижче -- і це головне в усьому
    // записі. Ті годують запасний шлях OZ_Identity.Get().OrgOf(), а той годує
    // AreHostile(), тобто рішення стріляти чи ні. Записати туди проекцію
    // Discord означало б рівно те, від чого застерігає шапка мода фракцій:
    // роль, зняту при мертвому мості, вже ніщо не зніме, і гра стрілятиме по
    // своїх ще довго після того, як у гільдії все виправили.
    //
    // Ці ж поля не читає ніхто, крім списку контактів, і той показує їх під
    // позначкою «застаріле».
    //
    // ДВІ ОСІ, а не одна (ТЗ-1 §3 R1.3): базова фракція й угруповання
    // застарівають нарізно, і показати «Сталкер-легенда» без угруповання --
    // правда, а показати одне замість іншого -- ні.
    string SeenBase    = "";
    string SeenOrg     = "";
    string SeenRank    = "";
    // Внутріфракційне звання -- окрема вісь від SeenRank (сталкерського).
    string SeenFRank   = "";
    ref array<string> SeenPosts;
    ref array<string> SeenTraits;

    // Епоха сесій. Пристрій пам'ятає, з якою епохою на ньому відкрилась
    // сесія, і вважається ОНЛАЙН, поки вона збігається з цією.
    //
    // «Скинути інші сесії» -- це просто += 1. Усі решта пристроїв мовчки
    // переходять в офлайн: вони не ламаються й не втрачають даних, вони
    // перестають оновлюватись, і їхній знімок замерзає на цій миті.
    //
    // Ніякого обліку чужих предметів, переживає рестарт, коштує одну
    // операцію -- а альтернатива вимагала б знати, де зараз кожен пристрій,
    // який гравець колись тримав у руках.
    int SessionEpoch = 1;

    // ПОКОЛІННЯ ПЕРСОНАЖА. Один Steam-акаунт -- багато життів у Зоні, і це
    // різні люди: після пермадесу старий запис заморожується у власний файл
    // (players\<uid>.g<N>.json), а тут починається новий -- з тим самим
    // Steam64 і чистим усім.
    //
    // Нуль у старому файлі означає перше покоління: до пермадесу все, що
    // існувало, було першим життям.
    int Gen = 1;

    // --- транспондер ---
    //
    // Кому видно твою позицію на чужій карті. НАБІР слагів, а не одне слово
    // (ТЗ-4 R-A3.1, R-A3.2, канон із 2026-09-02):
    //   []                       -- нікому
    //   ["public"]               -- усім у радіусі
    //   ["contacts"]             -- контактам (записник)
    //   ["faction"]              -- своїм по угрупованню
    //   ["faction", "contacts"]  -- і тим, і тим
    //
    // Порожній набір -- замовчування: маячок мовчить, доки гравець сам його
    // не увімкнув. Іншого безпечного замовчування тут немає.
    //
    // "faction" без мода фракцій не існує (R-A3.4): при завантаженні
    // знімається з набору з WARNING.
    //
    // Приймати чужі маячки й вести свій однаково потребує АНТЕНИ: радіус дає
    // саме вона. Без модуля антени транспондер не працює в обидва боки.
    //
    // Версія файла НЕ піднімається, якщо в ньому лишився старий ключ:
    // JsonFileLoader мовчки пропускає ключ, під який у класі немає поля, тож
    // живий файл зі старими ключами читається далі, а перший же запис їх
    // звідти прибирає.
    ref array<string> TransponderSet;

    // --- присутність ---
    //
    // Чи видно тебе в списку «хто зараз у Зоні». Це ІНША річ, ніж транспондер,
    // і плутати їх не можна:
    //
    //   транспондер -- твоя точка на чужій КАРТІ, у радіусі антени;
    //   присутність -- твоє ім'я в СПИСКУ онлайну, на весь сервер.
    //
    // Вимикачі незалежні: можна числитись у мережі й не світити позицію, і
    // навпаки -- вести маячок для двох контактів, лишаючись невидимим для
    // решти. Замовчування тут протилежне транспондеру: у списку видно, бо
    // список і є те, заради чого КПК носять.
    //
    // ДВА НЕЗАЛЕЖНІ ВИМИКАЧІ (ТЗ-4 R-A1.1), а не один:
    //   HiddenFromZone     -- мене немає серед присутніх у Зоні: у записниках
    //                         контактів я падаю в ОФЛАЙН;
    //   HiddenFromContacts -- мене немає в записниках узагалі, навіть офлайн.
    // Маячок на карті сюди не належить (R-A1.2): за нього відповідає
    // транспондер.
    //
    bool HiddenFromZone     = false;
    bool HiddenFromContacts = false;

    // --- друзі ---
    //
    // Дружба ВЗАЄМНА і потребує згоди обох. Тому списків два: прийняті друзі
    // й вхідні запити. Односторонній «друг» був би способом стежити за тим,
    // хто про це не знає.
    //
    // Просити в друзі можна лише ЗБЛИЗЬКА -- це перевіряє сервер. У Зоні
    // знайомляться в очі, а не за списком онлайну; заразом це знімає питання
    // «звідки клієнт узяв чужий Steam64»: нізвідки, він його не бачить.
    ref array<string> Friends;
    // NPC у контактах -- псевдо-uid-и "npc:<id>". СВІДОМО окремий простір
    // від SteamID: NPC не буває другом, членом групи чи ціллю обміну.
    ref array<string> NpcContacts;
    // ВХІДНІ ПРОПОЗИЦІЇ ОБМІНУ -- разом зі своїм строком.
    //
    // Строк лежав в окремій мапі в пам'яті сервера, і саме тому не працював
    // там, де потрібен найбільше: рестарт стирав мапу, а список у файлі
    // лишався, і кожна збережена пропозиція ставала «протухлою назавжди» --
    // зустрічний тик після рестарту не замикав обмін НІКОЛИ. Строк живе там
    // само, де пропозиція, і переживає рестарт разом із нею.
    ref array<ref OZ_FriendReq> FriendReq;

    // --- належність: ДВІ ОСІ ---
    //
    // Запасний шлях: коли на сервері немає мода фракцій, приналежність ставить
    // адмін або квестовий мод -- сюди. Живий постачальник ці поля перекриває,
    // і вони лишаються просто останнім відомим значенням.
    //
    // BaseFaction -- «сталкер». Є в кожного, ставить її гра при першому вході
    // (ТЗ-1 §7), і знімає тільки заводський скид персонажа. Проекція від моста
    // її НІКОЛИ не обнуляє: вона не з Discord.
    //
    // OrgFaction -- угруповання, не більше одного. Порожнє -- одинак. Це не
    // помилка й не «не задано», а найчастіший стан у Зоні, і окремого слова
    // для нього не треба.
    string BaseFaction = "";
    string OrgFaction  = "";

    override int LatestVersion()
    {
        return 3;
    }

    // ГІЛКИ ЧИТАННЯ СТАРОГО ФОРМАТУ ТУТ НЕМАЄ, І ЦЕ РІШЕННЯ, А НЕ ПРОГАЛИНА.
    //
    // 2 -> 3 розщепило Faction на BaseFaction і OrgFaction, а SeenFaction на
    // SeenBase і SeenOrg. Прочитати старе поле нікуди: одне значення не знає,
    // якою з двох осей воно було, і будь-яка спроба вгадати поставила б
    // угруповання тому, хто просто сталкер, або навпаки.
    //
    // Платити за це не треба: власник зняв вимогу зворотної сумісності
    // 2026-09-01 -- мод живе тільки на дев-стенді, старі файли гравців просто
    // видаляються (ТЗ-1 §11). Версія піднята саме для того, щоб старий файл
    // був ВИДНО, а не тихо прочитаний наполовину.
    //
    // Власного override Migrate() при цьому не треба зовсім: «підняти версію
    // й нічого не читати» -- це рівно те, що робить база OZ_ConfigBase.

    override void LoadDefaults()
    {
        Version      = LatestVersion();
        SteamId      = "";
        DiscordId    = "";
        FirstSeen    = "";
        LastSeen     = "";
        SessionEpoch = 1;

        // ПОКОЛІННЯ Й ЗНІМОК РОЛЕЙ -- ТЕЖ ТУТ.
        //
        // Договір, під яким цей метод кличуть, сказаний у OZ_ConfigBase:
        // «LoadDefaults МУСИТЬ виставити КОЖНЕ поле: його кличуть і на
        // порожньому об'єкті, і поверх частково прочитаного сміття після
        // невдалого розбору». Шість полів у ньому бракувало, і саме друга
        // половина договору робила це небезпечним: після невдалого розбору
        // Gen лишався тим, що встиг прочитатись зі сміття, -- а Gen вирішує,
        // яке покоління персонажа вважати живим, тобто чиї контакти й чиї
        // КПК належать цій людині.
        Gen        = 1;
        SeenBase    = "";
        SeenOrg     = "";
        SeenRank    = "";
        SeenFRank   = "";
        SeenPosts   = new array<string>();
        SeenTraits  = new array<string>();

        TransponderSet  = new array<string>();
        HiddenFromZone     = false;
        HiddenFromContacts = false;

        Name      = "";
        Friends   = new array<string>();
        NpcContacts = new array<string>();
        FriendReq = new array<ref OZ_FriendReq>();
        BaseFaction = "";
        OrgFaction  = "";
    }

    override void Validate(out int warnings)
    {
        warnings = 0;

        // Масиви, яких немає у старому файлі, мусять з'явитись ТУТ, а не при
        // першому зверненні: інакше кожен, хто їх читає, зобов'язаний
        // перевіряти на null, і рано чи пізно хтось забуде.
        if (!Friends)
            Friends = new array<string>();
        if (!FriendReq)
            FriendReq = new array<ref OZ_FriendReq>();
        if (!NpcContacts)
            NpcContacts = new array<string>();

        // ВКЛАДЕНЕ -- У СТВОРЕНЕ СКРИПТОМ (шапка OZ_ConfigBase). Порожній
        // запис замість null: строк без ключа інертний, а null у списку валив
        // би кожного, хто по ньому проходить.
        for (int fi = 0; fi < FriendReq.Count(); fi++)
        {
            if (FriendReq[fi])
                FriendReq.Set(fi, FriendReq[fi].Copy());
            else
                FriendReq.Set(fi, new OZ_FriendReq());
        }
    }
}

class OZ_PlayerStore
{
    // Контейнери -- одразу при оголошенні. Ensure() перед кожним зверненням
    // був восьмим таким у ядрі й нічого не давав: статики Enforce ініціюються
    // при оголошенні так само надійно (те саме вже робили OZ_BridgeClient і
    // OZ_Identity).
    private static ref map<string, ref OZ_PlayerData> s_Cache = new map<string, ref OZ_PlayerData>();
    private static ref array<string>                  s_Dirty = new array<string>();

    // Чий файл ми НЕ ЗМОГЛИ прочитати й НЕ ЗМОГЛИ винести в карантин.
    //
    // На диску лежить єдиний примірник даних цієї людини, а в пам'яті --
    // дефолти. Писати їх поверх означало б стерти акаунт через хвилинний збій
    // ФС; тому запис таким uid заборонений до кінця сеансу цього запису.
    private static ref array<string> s_Locked = new array<string>();

    private static string PathOf(string uid)
    {
        return OZ_Const.PLAYERS_DIR + "\\" + uid + ".json";
    }

    // Чи можна взагалі клеїти цей uid у дорогу.
    //
    // PathOf підставляє його БЕЗ перевірок, тож порожній рядок давав файл
    // `players\.json`, а рядок із роздільником -- запис куди завгодно під
    // профілем сервера. Uid приходить від CF і від чужих модів; на 17 цифр
    // не перевіряємо (псевдо-uid NPC і тестові акаунти стенду теж мають тут
    // жити), а от роздільник шляху -- не uid у жодному написанні.
    private static bool Nameable(string uid)
    {
        if (uid == "")
            return false;
        if (uid.IndexOf("\\") != -1)
            return false;
        if (uid.IndexOf("/") != -1)
            return false;
        if (uid.IndexOf("..") != -1)
            return false;
        return true;
    }

    private static string GravePathOf(string uid, int gen)
    {
        return OZ_Const.PLAYERS_DIR + "\\" + uid + ".g" + gen.ToString() + ".json";
    }

    static OZ_PlayerData Load(string uid)
    {
        if (s_Cache.Contains(uid))
            return s_Cache.Get(uid);

        // Непридатний uid НЕ КЕШУЄМО й на диск не носимо: віддаємо дефолти,
        // щоб викликач не впав на null, і кажемо про це один раз.
        if (!Nameable(uid))
        {
            OZ_Log.Warn("player store: refusing to touch a file for uid \"" + uid + "\"");
            OZ_PlayerData empty = new OZ_PlayerData();
            empty.LoadDefaults();
            return empty;
        }

        OZ_PlayerData d = new OZ_PlayerData();
        // backup=false: файлів гравців сотні, і копія кожного перед кожним
        // записом засмітила б Backup так, що знайти в ньому щось стало б
        // неможливо. Резервні копії -- для конфігів адміна.
        bool writable = OZ_ConfigLoader<OZ_PlayerData>.Load(PathOf(uid), "player_" + uid, d, false);

        int locked = s_Locked.Find(uid);
        if (!writable && locked == -1)
            s_Locked.Insert(uid);
        else if (writable && locked != -1)
            s_Locked.Remove(locked);

        d.SteamId = uid;

        // У КЕШ -- ПЕРЕД MarkDirty, і це не косметика.
        //
        // MarkDirty відмовляє тому, кого немає в кеші, і пише про це рядком
        // рівнем Dbg: правило правильне (позначити брудним незавантаженого
        // означало б обіцяти запис, якого нема кому зробити). Але тут воно
        // зривалось на самому першому вході КОЖНОГО нового гравця: штамп
        // FirstSeen ставився в об єкт, який у кеш ще не поклали, -- і мовчки
        // не доїжджав до диска. Гравець лишався без дати першої появи, поки
        // його файл не зробить брудним щось інше.
        //
        // Той самий різновид помилки вже ловили у Flush (там s_Dirty чистили
        // ПІСЛЯ перевірки кешу), і фіксується він тим самим: спершу зробити
        // запис відомим, потім про нього щось стверджувати.
        s_Cache.Insert(uid, d);

        // Нормалізація набору транспондера (ТЗ-4 A1/A3) і зняття "faction"
        // на сервері без мода фракцій. Це РЕМОНТИ, а не міграції: обидва
        // залежать від стану, а не від версії файла, і мусять іти на кожному
        // завантаженні. Після вставки в кеш -- інакше MarkDirty відмовить.
        if (OZ_PlayerFlags.Upgrade(d))
            MarkDirty(uid);

        // ШТАМПА FirstSeen ТУТ БІЛЬШЕ НЕМАЄ -- він переїхав у
        // OZ_Module.OnInvokeConnect, тобто в те єдине місце, де ми ТОЧНО
        // знаємо, що це справді гравець і він справді зайшов.
        //
        // Тут він заводив файл кожному uid, який хтось колись спитав:
        // офлайновому контакту з чужого записника, ключу зі старого
        // повідомлення, будь-чому. Каталог players\ обростав людьми, яких на
        // сервері не бувало.
        return d;
    }

    // БРУДНИМ МОЖЕ БУТИ ЛИШЕ ТЕ, ЩО В КЕШІ.
    //
    // Позначка означає «в пам'яті лежить свіжіше за диск». Для uid, якого в
    // кеші немає, це твердження порожнє: писати нічого. А запис у черзі
    // лишався б у ній назавжди -- Flush мовчки виходив, не прибираючи його, --
    // і цикл FlushAll крутився б на ньому ВІЧНО, вішаючи сервер на скиданні.
    static void MarkDirty(string uid)
    {
        if (!s_Cache.Contains(uid))
        {
            OZ_Log.Dbg("player " + uid + ": marked dirty while not loaded, ignored");
            return;
        }

        if (s_Dirty.Find(uid) == -1)
            s_Dirty.Insert(uid);
    }

    static void Flush(string uid)
    {
        // З ЧЕРГИ ПРИБИРАЄМО В БУДЬ-ЯКОМУ РАЗІ, і це головне в цій функції.
        //
        // Раніше вихід «немає в кеші» стояв ПЕРЕД зняттям позначки, тож
        // FlushAll не мав жодного способу дійти до кінця: s_Dirty[0] не
        // зникав, лічильник не спадав, цикл не закінчувався.
        int i = s_Dirty.Find(uid);
        if (i != -1)
            s_Dirty.Remove(i);

        if (!s_Cache.Contains(uid))
            return;

        // ФАЙЛ, ЯКОГО МИ НЕ ЗРОЗУМІЛИ, НЕ ПЕРЕПИСУЄМО ДЕФОЛТАМИ.
        if (s_Locked.Find(uid) != -1)
        {
            string no = "player " + uid;
            no += ": file could not be read and could not be quarantined";
            no += " - refusing to overwrite it with defaults";
            OZ_Log.Error(no);
            return;
        }

        OZ_ConfigLoader<OZ_PlayerData>.Save(PathOf(uid), "player_" + uid, s_Cache.Get(uid), false);
    }

    static void FlushAll()
    {
        while (s_Dirty.Count() > 0)
            Flush(s_Dirty[0]);
    }

    // Скинути порцію -- але так, щоб уся черга розійшлась за `ticks` тіків.
    //
    // FlushAll писав усе брудне одним синхронним циклом раз на тридцять
    // секунд, а брудними після ресинку ролей стають майже всі онлайн
    // одночасно -- тобто вісім десятків файлів в одному кадрі на сервері з
    // одним ядром. Порції цей хитч прибирають, але сама лише стала порція
    // СТЕЛЮ ВТРАТИ ПІДНІМАЛА: вісім за тік означали, що вісімдесят брудних
    // файлів пишуться десять тіків, тобто п'ятдесят секунд проти тридцяти
    // до всієї цієї роботи. Комент при цьому казав, що стеля не міняється,
    // -- і був неправдою.
    //
    // Тому дно, а не стеля: беремо щонайменше n, а як черга довша -- стільки,
    // щоб вона скінчилась за `ticks` тіків. Тридцять секунд лишаються
    // тридцятьма при будь-якій довжині черги, а хитч росте лише тоді, коли
    // альтернатива йому -- втрачені дані.
    static void FlushSome(int n, int ticks)
    {
        if (ticks < 1)
            ticks = 1;

        // Округлення ВГОРУ: інакше черга з семи при восьми тіках дала б нуль
        // за тік і не розійшлась би ніколи. n нижче все одно не менший, але
        // покладатись на це не варто.
        int need = (s_Dirty.Count() + ticks - 1) / ticks;
        if (need > n)
            n = need;

        int done = 0;
        while (s_Dirty.Count() > 0 && done < n)
        {
            Flush(s_Dirty[0]);
            done++;
        }
    }

    // Вивантажує з пам'яті, дописавши на диск. Кличеться на дисконекті:
    // тримати в кеші того, хто пішов, немає сенсу.
    static void Unload(string uid)
    {
        Flush(uid);
        if (s_Cache.Contains(uid))
            s_Cache.Remove(uid);

        int locked = s_Locked.Find(uid);
        if (locked != -1)
            s_Locked.Remove(locked);
    }

    // ------------------------------------------------- офлайнове читання
    //
    // ПОГЛЯНУТИ на запис, не беручи його в кеш назавжди.
    //
    // Load() тримає все, що колись питали, до кінця сеансу: Unload() знімає
    // лише того, хто сам вийшов. А офлайнові uid питають на КОЖНЕ малювання
    // списку контактів -- чужі друзі, чужі друзі друзів, -- і кеш ріс, поки
    // сервер стояв. Гірше: Load ще й штампував FirstSeen випадковому uid і
    // заводив йому файл, тобто створював запис людині, яка сюди не заходила.
    //
    // Peek читає крізь маленький кеш зі строком: нічого не брудниться,
    // нічого не штампується, файл не заводиться. Хто зараз у Зоні -- той у
    // s_Cache, і Peek віддає саме його, не читаючи диска вдруге.
    private static ref map<string, ref OZ_PlayerData> s_Peek   = new map<string, ref OZ_PlayerData>();
    private static ref map<string, int>               s_PeekAt = new map<string, int>();

    private static const int PEEK_TTL_MS = 300000;   // п'ять хвилин
    private static const int PEEK_MAX    = 256;

    static OZ_PlayerData Peek(string uid)
    {
        if (!Nameable(uid))
            return null;

        if (s_Cache.Contains(uid))
            return s_Cache.Get(uid);

        int now = GetGame().GetTime();

        int at;
        if (s_PeekAt.Find(uid, at) && (now - at) < PEEK_TTL_MS)
            return s_Peek.Get(uid);

        // Файла немає -- людини теж. Порожній запис тут гірший за null:
        // викликач мусить бачити різницю між «нічого не знаємо» й «знаємо,
        // що порожньо».
        OZ_PlayerData d;
        if (FileExist(PathOf(uid)))
        {
            d = new OZ_PlayerData();
            OZ_ConfigLoader<OZ_PlayerData>.Load(PathOf(uid), "player_" + uid, d, false, false);
            d.SteamId = uid;
        }

        // Скидаємо цілком, а не найстаріше: кеш показу, а не сховище, і
        // другий прохід по мапі заради LRU коштував би більше, ніж економив.
        if (s_Peek.Count() >= PEEK_MAX)
        {
            s_Peek.Clear();
            s_PeekAt.Clear();
        }

        s_Peek.Set(uid, d);
        s_PeekAt.Set(uid, now);
        return d;
    }

    // ------------------------------------------- ключ персонажа
    //
    // Один Steam-акаунт -- багато життів, і в записнику це РІЗНІ ЛЮДИ. Тому
    // контакти, запити й транспондер зберігають не Steam64, а КЛЮЧ
    // ПЕРСОНАЖА: "<uid>#<покоління>".
    //
    // Голий uid у старому файлі означає перше покоління -- до пермадесу
    // інших і не було.
    // ЧИТАЄМО ЧЕРЕЗ Peek, а не Load: ключ персонажа питають і про того, кого
    // на сервері немає -- контакт із чужого записника, адресат старого
    // повідомлення. Load тримав би кожного такого в кеші до кінця сеансу.
    static string KeyOf(string uid)
    {
        OZ_PlayerData d = Peek(uid);
        if (!d)
            return uid + "#1";
        return uid + "#" + d.Gen.ToString();
    }

    static string UidOfKey(string key)
    {
        int at = key.IndexOf("#");
        if (at == -1)
            return key;
        return key.Substring(0, at);
    }

    static int GenOfKey(string key)
    {
        int at = key.IndexOf("#");
        if (at == -1)
            return 1;
        return key.Substring(at + 1, key.Length() - at - 1).ToInt();
    }

    // Чи це ЖИВЕ покоління -- той самий персонаж, а не той, ким цей акаунт
    // був колись. Заморожений контакт не буває онлайн, не оновлює фракцію й
    // не отримує повідомлень.
    static bool IsLive(string key)
    {
        string uid = UidOfKey(key);
        if (uid == "")
            return false;

        OZ_PlayerData d = Peek(uid);
        if (!d)
            return false;

        return GenOfKey(key) == d.Gen;
    }

    // Заморожений запис минулого покоління -- з диска, ОДИН раз.
    //
    // Читається рідко (лише коли в чиємусь записнику лишився старий
    // персонаж), тож кеш маленький і живе до кінця сеансу.
    private static ref map<string, ref OZ_PlayerData> s_Frozen = new map<string, ref OZ_PlayerData>();

    static OZ_PlayerData FrozenOf(string key)
    {
        if (s_Frozen.Contains(key))
            return s_Frozen.Get(key);

        string path = GravePathOf(UidOfKey(key), GenOfKey(key));

        OZ_PlayerData d;
        if (FileExist(path))
        {
            d = new OZ_PlayerData();
            OZ_ConfigLoader<OZ_PlayerData>.Load(path, "grave_" + key, d, false, false);
        }

        s_Frozen.Set(key, d);
        return d;
    }

    // Запис ЗА КЛЮЧЕМ: живий або заморожений. Може бути null -- покоління
    // є в чиємусь записнику, а файла вже немає (адмін прибрав руками).
    //
    // ЩО ЗВІДСИ ПОВЕРТАЄТЬСЯ -- ДЛЯ ЧИТАННЯ. Живий і присутній гравець
    // прийде з кеша, як і раніше; відсутнього віддає Peek, і правки в ньому
    // на диск не поїдуть (MarkDirty відмовить тому, кого немає в кеші, --
    // так само, як відмовляв і замороженому запису досі).
    static OZ_PlayerData ByKey(string key)
    {
        if (IsLive(key))
            return Peek(UidOfKey(key));
        return FrozenOf(key);
    }

    // ---------------------------------------------------- пермадес
    //
    // ЗАМОРОЗИТИ запис і почати наступний. Старе життя лишається на диску
    // цілим -- окремим файлом із номером покоління, -- а живий запис іде
    // далі вже як новий персонаж.
    //
    // Копія, а не перейменування: живий файл мусить лишатись на місці, бо
    // Steam64 не змінився й наступний Load піде саме за ним.
    static void Freeze(string uid)
    {
        OZ_PlayerData d = Load(uid);
        if (!d)
            return;

        // На диск -- ПЕРЕД копіюванням: у пам'яті може бути свіжіше.
        MarkDirty(uid);
        Flush(uid);

        string grave = GravePathOf(uid, d.Gen);
        if (FileExist(PathOf(uid)))
        {
            if (!CopyFile(PathOf(uid), grave))
                OZ_Log.Warn("player " + uid + ": could not freeze generation " + d.Gen.ToString());
            else
                OZ_Log.Info("player " + uid + ": generation " + d.Gen.ToString() + " frozen as " + grave);
        }

        d.Gen = d.Gen + 1;
        MarkDirty(uid);
        Flush(uid);

        // Знімок показу застарів разом із поколінням.
        if (s_Peek.Contains(uid))
        {
            s_Peek.Remove(uid);
            s_PeekAt.Remove(uid);
        }
    }

    // CachedCount() ТУТ БІЛЬШЕ НЕМАЄ: діагностичний лічильник без жодного
    // викликача в усій серії.
}

// Приведення прапорців гравця до канону ТЗ-4 §A при завантаженні файла.
//
// Тут лишилось те, що стосується ЖИВОГО набору й мусить статись на кожному
// завантаженні, а не один раз, -- це РЕМОНТ, а не міграція.
//
// Окремий клас, а не гілка Migrate(): версія файлу не міняється. Повертає
// true, якщо файл треба записати.
class OZ_PlayerFlags
{
    static bool Upgrade(OZ_PlayerData d)
    {
        bool changed = false;

        if (!d.TransponderSet)
        {
            d.TransponderSet = new array<string>();
            changed = true;
        }

        // R-A3.4: режиму "faction" без мода фракцій НЕ ІСНУЄ. Не «тихо не
        // працює», а знімається -- і про це кажуть.
        if (d.TransponderSet.Find("faction") != -1 && !OZ_Identity.Present())
        {
            d.TransponderSet.RemoveItem("faction");
            OZ_Log.Warn("player " + d.SteamId + ": transponder mode 'faction' dropped - there is no faction mod on this server");
            changed = true;
        }

        return changed;
    }
}
