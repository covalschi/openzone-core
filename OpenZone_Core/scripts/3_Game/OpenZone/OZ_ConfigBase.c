// База версіонованого конфігу і загальний порядок його завантаження.
//
// Головне правило, від якого тут усе залежить: КОНФІГ НІКОЛИ НЕ Є ПРИЧИНОЮ НЕ
// ЗАВАНТАЖИТИСЬ. Зіпсований файл, застаріла схема, безглузде значення -- усе це
// дає запис у лог і дефолт, але сервер піднімається. Мод, який відмовляється
// стартувати через кому не в тому місці, адміну не потрібен.
//
// ------------------------------------------------------------------------
// ОБ'ЄКТ, ЯКИЙ ВИДІЛИВ СЕРІАЛІЗАТОР, НЕ МАЄ ЖОДНОГО ІНІЦІАЛІЗАТОРА ПОЛІВ
// (зміряно 2026-09-06, persistence-networking.md). Це друге правило цього
// файла, і воно визначає, що мусить робити Validate().
//
// JsonFileLoader -- тонка обгортка над рідним JsonSerializer.ReadFromString,
// а той оголошений як `proto bool ReadFromString(void variable_in, ...)`:
// КОРЕНЕВИЙ об'єкт він заповнює на місці, той самий, що йому дали. Тому
// корінь тут безпечний -- його створює скриптовий `new` у викликача (кожен
// без винятку: OZ_Settings.ServerLoad, OZ_Spawns.Reload, OZ_PlayerStore), і
// поле, якого у файлі немає, лишається зі своїм ініціалізатором.
//
// А от УСЕ ВКЛАДЕНЕ серіалізатор виділяє САМ -- і `ref`-поле, і кожен
// елемент масиву, і навіть тоді, коли розділу у файлі немає зовсім (він
// повертається створеним і обнуленим). Конструктор скрипта при цьому не
// кличеться, тобто `string Kind = ""` і `int MaxMembers = 50` НЕ виконуються.
// Кожен член, якому серіалізатор не знайшов значення у файлі, -- сира
// пам'ять: одразу після розбору сторінка ще обнулена й читається правильно,
// а через хвилину на тому самому місці лежать чужі байти. Найзвичайніший
// шлях у цю пастку -- не зіпсований файл, а НОВЕ ПОЛЕ В КЛАСІ: живі файли
// його не несуть, LoadDefaults на успішному розборі не кличеться, і поле
// живе сирим.
//
// ІДІОМА, ЯКОЇ ЦЕ ВИМАГАЄ ВІД КОЖНОГО МОДА СЕРІЇ. Дай кожному вкладеному
// класу метод Copy(), який створює `new` свого типу й переписує в нього поле
// за полем (а вкладені в себе -- їхніми Copy()), і поклич його з Validate():
//
//     if (!Bridge)
//         Bridge = new OZ_BridgeSettings();
//     else
//         Bridge = Bridge.Copy();
//
// Validate() -- єдине правильне місце: лоадер кличе його ОДРАЗУ після
// розбору, тобто поки читання ще чесне. Після цього об'єкт зроблено
// скриптом, його ініціалізатори виконались, і `== ""` нижче по коду знову
// означає те, що написано. Ціна -- одна копія на завантаження.
//
// Умовчання вкладеного поля, якого у файлі не було, після копії -- НУЛЬ, а
// не те, що стоїть в ініціалізаторі: копія переносить чесно прочитаний нуль.
// Тому справжні умовчання вкладених полів виставляє сам Validate(), і в цьому
// файлі так і зроблено (Bridge.ServerId, Staging.Radius, Zones[i].Center).
// ------------------------------------------------------------------------

// ЯКИЙ САМЕ ФАЙЛ ЗАРАЗ ПЕРЕВІРЯЮТЬ -- і навіщо це комусь знати.
//
// Після розбору «ключа у файлі немає» й «у ключі нуль» -- один і той самий
// нуль (шапка вище), а бувають ключі, де ці два означають протилежне:
// Health01 предмета спорядження -- «не чіпати» проти «вбита річ». Розсудити
// їх може лише сирий текст того самого файла, а шлях до нього знає єдиний
// тут -- лоадер.
//
// Тому лоадер називає файл рівно на час Validate(), і лише тоді, коли об'єкт
// у пам'яті справді прочитаний із НЬОГО (на дефолтах текст файла описує не
// те, що лежить у пам'яті). Поза цим вікном Path() порожній, і той, хто
// питає, нічого не домислює.
//
// Читає це сьогодні одне місце -- OZ_LoadoutKeys у 4_World; тут стоїть сама
// лише поличка, бо 4_World для 3_Game ще не існує.
class OZ_ConfigFile
{
    private static string s_Path = "";

    // Покоління: те саме завантаження чи вже наступне. Гарячий перечит того
    // самого шляху мусить перечитати й текст -- інакше читач віддавав би
    // відповіді про файл, якого на диску вже немає.
    private static int s_Gen = 0;

    static void Begin(string path)
    {
        s_Path = path;
        s_Gen++;
    }

    static void End()
    {
        s_Path = "";
    }

    static string Path()
    {
        return s_Path;
    }

    static int Gen()
    {
        return s_Gen;
    }
}

class OZ_ConfigBase
{
    int Version;

    // Нащадки перевизначають усі чотири.
    //
    // LoadDefaults МУСИТЬ виставити КОЖНЕ поле: його кличуть і на порожньому
    // об'єкті, і поверх частково прочитаного сміття після невдалого розбору.
    //
    // Validate МУСИТЬ пересадити КОЖЕН вкладений об'єкт, який конфіг лишає
    // собі, у створений скриптом -- або виставити кожен його член сам. Чому
    // саме так -- у шапці цього файла.
    int  LatestVersion()            { return 1; }
    void LoadDefaults()             { Version = LatestVersion(); }
    bool Migrate(int from)          { Version = LatestVersion(); return true; }
    void Validate(out int warnings) { warnings = 0; }
}

class OZ_ConfigLoader<Class T>
{
    // cfg приходить УЖЕ створеним. new T() всередині дженерика в Enforce
    // ненадійний, тому конкретний тип створює викликач -- саме так це зроблено
    // і в ZP_Research, тільки там без дженерика зовсім.
    //
    // Порядок: прочитати -> мігрувати -> перевірити -> перезаписати, якщо щось
    // змінилось. Провал будь-якого кроку відкидає на дефолти й відкладає
    // зіпсоване вбік.
    //
    // ПОВЕРТАЄ, ЧИ МОЖНА ЦЕЙ ФАЙЛ ПИСАТИ, і це головне в усьому класі.
    //
    // Load сам сумлінно не чіпав диск, коли не зміг ані прочитати файл, ані
    // винести його в карантин, -- а викликачі про це не знали. OZ_PlayerStore
    // після повернення робив об'єкт брудним, і найближчий Flush() перетирав
    // той самий нечитаний файл дефолтами: DiscordId, друзі, покоління --
    // геть. Тимчасовий збій ФС (файл тримає антивірус) від справжньої порчі
    // не відрізнити, тож ціна помилки -- акаунт гравця.
    //
    // false означає «на диску лежить єдиний примірник, якого ми не зрозуміли;
    // не пиши поверх нього нічого». Викликач, який ігнорує відповідь,
    // поводиться як раніше -- тобто нічого не ламає в тих, хто писати й не
    // збирався.
    //
    // rewriteOnFix=false вимикає перезапис, спричинений самим лише Validate:
    // адмінський шлях перечитує файл ПЕРЕД кожним власним записом, і зайвий
    // запис із бекапом там затирає .bak ще до правки, заради якої його й
    // роблять.
    static bool Load(string path, string tag, inout T cfg, bool backupOnWrite = true, bool rewriteOnFix = true)
    {
        // NULL -- ВІДМОВА, А НЕ «НЕХАЙ СЕРІАЛІЗАТОР СТВОРИТЬ САМ».
        //
        // Створить: рідний ReadFromString виділяє корінь, якого йому не дали.
        // Але виділяє ВІН, отже жоден ініціалізатор поля не виконається, і
        // весь конфіг стане тим, від чого стереже шапка цього файла, -- у
        // кожному полі, а не лише у вкладених. Дешевше відмовитись голосно.
        //
        // false -- це «не пиши поверх файла»: викликач із порожнім конфігом
        // у пам'яті нічого туди й не покладе.
        if (!cfg)
        {
            OZ_Log.Error("config " + tag + ": nothing to load into - the caller must pass a constructed object");
            return false;
        }

        bool fresh = false;
        bool salvaged = false;
        // Чи справді те, що зараз у пам'яті, прочитане з ЦЬОГО файла. На
        // дефолтах -- ні, і текст файла тоді описує не той об'єкт
        // (див. OZ_ConfigFile).
        bool fromFile = true;
        // Карантин не вдався -- файл на диску НЕ ЧІПАЄМО взагалі. Інакше
        // єдиний примірник того, що зламав адмін, зникає назавжди, і в лозі
        // лишається сама лише назва помилки розбору.
        bool keepFile = false;
        string err;

        if (!FileExist(path))
        {
            cfg.LoadDefaults();
            fresh = true;
            fromFile = false;
        }
        else if (!ReadTwice(path, cfg, err))
        {
            OZ_Log.Error("read " + path + ": " + err);
            bool kept = OZ_Json.Quarantine(path, tag);
            cfg.LoadDefaults();
            fresh = true;
            salvaged = true;
            fromFile = false;
            if (!kept)
                keepFile = true;
        }
        else if (cfg.Version > cfg.LatestVersion())
        {
            // ФАЙЛ ІЗ МАЙБУТНЬОГО -- НЕ ПОЛАМАНИЙ ФАЙЛ.
            //
            // Version != Latest трактувалось симетрично, і відкат білда
            // сервера ліз у Migrate(), який уміє лише `from < N`: той штампував
            // LatestVersion і переписував живий файл, викидаючи з нього кожне
            // поле, якого старий білд не знає. Бекап лишався, але зникнення
            // полів із ЖИВОГО файла адмін помічав уже наслідками.
            //
            // Ані карантину, ані запису: файл на місці й цілий, а сервер
            // піднімається на дефолтах -- рівно як обіцяє шапка цього файла.
            string ahead = "config " + tag;
            ahead += " is v" + cfg.Version;
            ahead += ", newer than this build's v" + cfg.LatestVersion();
            ahead += " - loading defaults and leaving the file untouched";
            OZ_Log.Error(ahead);
            cfg.LoadDefaults();
            keepFile = true;
            salvaged = true;
            fromFile = false;
        }
        else if (cfg.Version != cfg.LatestVersion())
        {
            int from = cfg.Version;
            if (!cfg.Migrate(from))
            {
                string bad = "cannot migrate " + tag;
                bad += " from v" + from;
                OZ_Log.Error(bad);
                bool keptM = OZ_Json.Quarantine(path, tag);
                cfg.LoadDefaults();
                salvaged = true;
                fromFile = false;
                if (!keptM)
                    keepFile = true;
            }
            else
            {
                string ok = "migrated " + tag;
                ok += " v" + from;
                ok += " -> v" + cfg.Version;
                OZ_Log.Info(ok);
            }
            fresh = true;
        }

        // ФАЙЛ НАЗИВАЄТЬСЯ РІВНО НА ЧАС VALIDATE (див. OZ_ConfigFile): усе, що
        // мусить відрізнити відсутній ключ від написаного нуля, читає текст
        // саме тут -- поки об'єкт у пам'яті прочитаний із цього файла й ще
        // ніхто його не полагодив.
        if (fromFile)
            OZ_ConfigFile.Begin(path);

        int warnings;
        cfg.Validate(warnings);

        OZ_ConfigFile.End();

        // ПОЧИНКИ VALIDATE ТЕЖ ЇДУТЬ НА ДИСК.
        //
        // Раніше писалось лише `fresh` -- тобто новий файл, невдалий розбір і
        // міграція. Файл правильної версії, який Validate тихо полагодив,
        // лишався на диску зламаним, і та сама починка повторювалась КОЖЕН
        // старт. Для настройок це не косметика: серед таких починок є та, що
        // вимикає міст (Bridge.Enabled = 0 при порожньому Url), і адмін бачив
        // мовчазно вимкнений міст щоразу, полагодивши файл.
        bool write = fresh;
        if (warnings > 0 && rewriteOnFix)
            write = true;

        // Після карантину резервну копію НЕ оновлюємо. Інакше .bak затерся б
        // тим самим сміттям, що вже лежить у .bad, і єдина копія, з якої можна
        // було відновитись, зникла б через один зіпсований старт.
        if (write && !keepFile)
            Save(path, tag, cfg, backupOnWrite && !salvaged);

        return !keepFile;
    }

    // Читаємо ДВІЧІ, перш ніж називати файл зіпсованим.
    //
    // Розбір ламає і кома не там, і файл, який цієї секунди тримає антивірус
    // або редактор адміна. Перше -- назавжди, друге -- на мить, а наслідок у
    // них був один: карантин і дефолти. Друга спроба коштує міліметр часу на
    // старті й нічого не коштує в звичайному разі, коли перша вдалася.
    private static bool ReadTwice(string path, inout T cfg, out string err)
    {
        if (JsonFileLoader<T>.LoadFile(path, cfg, err))
            return true;

        string err2;
        if (JsonFileLoader<T>.LoadFile(path, cfg, err2))
        {
            OZ_Log.Warn("read " + path + ": failed once (" + err + "), succeeded on retry");
            return true;
        }

        return false;
    }

    // backup=false для файлів гравців: їх сотні, і копія кожного перед кожним
    // записом засмітила б Backup так, що знайти в ньому щось стане неможливо.
    static void Save(string path, string tag, T cfg, bool backup = true)
    {
        if (backup && FileExist(path))
            OZ_Json.Backup(path, tag);

        string err;
        if (!JsonFileLoader<T>.SaveFile(path, cfg, err))
            OZ_Log.Error("write " + path + ": " + err);
    }
}
